import React from 'react'
import { Genre } from '../Songs/Genre';

import { Loading } from '../../Loading/Loading'



export const ContainerGenre = props => {
    const { genreMusic } = props
    console.log(genreMusic)
    return (
        <div>
          {
          
          }
        </div>
    )
}